# PLAN：一个月完整论文

> 本文件是唯一的执行计划。窗口、清单、周验收和每天任务都在这里。

> 窗口：**2026-09-22～2026-10-22**（31 天）。  
> 每天按 **8 小时**计：上午 3 小时、下午 3 小时、晚上 2 小时。10 月 1 日～7 日照常。  
> 10 月 22 日交付的是**可投稿的完整英文稿**。录用和 EI 检索在投稿之后，不排进这 31 天。

完整指下面清单全部写进正文，不因为日历变短而改成简报。篇幅服从目标会刊模板（常见 IEEE 双栏 4–6 页）：超页时压缩句子，不删实验种类。

---

## 1. 完整论文清单

交稿时逐项要有，缺一项不算完成。

| 部分 | 完成标准 |
|------|----------|
| 题名、摘要、关键词 | 摘要 150–250 词；关键词 4–6 个；摘要中的每个数字都能在表里找到 |
| 引言 | ≥4 段；编号贡献 3 条：分级定义、对照（含先定位再分级）、何时失效 |
| 相关工作 | 三小节：表面缺陷、有序分类、成色/外观质检。全文参考文献 ≥25，其中 ≥8 篇为 2021 年及以后 |
| 方法 | 三小节：3 档书面规则、整图分类与有序损失、先定位再分级的数据流。图 1 为流水线 |
| 主实验 | 同一公开集、同一测试划分。行至少包括：多数类、线性探测、预训练微调、有序损失或强增广。列：Accuracy、macro-F1、每档 F1、有序误差（MAE、差一档比例、差两档比例） |
| 定位臂 | 与最佳整图模型比上述指标；再按损伤尺度分小/大两组。有框或掩膜时另报定位 mAP@0.5 或 IoU。提升有限也保留 |
| 消融 | 至少两个开关：有序损失开/关、增广开/关 |
| 失败案例 | 测试集错例清单；类型至少覆盖反光或脏纹理误报、小裂纹漏检、背景当伤、相邻档混淆。论文图版 ≥8 张，每张有真值、预测、类型 |
| 真实小集 | 一类产品 ≥90 张、每档 ≥30。其中 ≥30 张双人标注，报告一致率和 Cohen's kappa。冻结模型只评测、不据此再选模型。写出相对公开测试集掉多少点，并按反光/杂乱背景分组 |
| 局限与结论 | 反光、跨产品、标注主观、公开集标签映射不等于真实成色 |
| 复现 | 主表每一行能对上命令、种子、机器、CSV 路径 |
| 图表 | 图 ≥4（流水线、两张混淆矩阵、失败图版、真实样例），表 ≥3（主结果、消融或有序误差、真实集）。全部被正文引用 |

课题边界仍按 `docs/01-direction.md`：主输出是 3 档等级。视频、0–10 连续分、全品类本体、同物检索、把新网络当贡献，不进入本稿。

---

## 2. 周验收

| 周 | 日期 | 本周结束时必须同时成立 |
|----|------|------------------------|
| 0 | 09-22～09-28 | 3 档规则写死；环境有 val 准确率；划分可复现；多数类 + 微调两行数字；有序误差三个数；引言英文写完；笔记 ≥8 |
| 1 | 09-29～10-05 | 主表 ≥4 行；定位再分级有对比数字和尺度分组；消融两个开关；方法三节 + 相关工作三节有稿；图 1 有草稿 |
| 2 | 10-06～10-12 | 失败清单与 ≥8 张图版；真实集 90 张、kappa、掉点；实验、局限、结论、摘要写完；**全部数字冻结** |
| 3 | 10-13～10-19 | 模板全文 PDF；图表终稿；参考文献达标；复现索引；自查清单无空项 |
| 4 | 10-20～10-22 | 终稿 PDF 与投稿回执 |

第 0 周若微调仍不超过多数类：第 1 周照跑全部对照和定位臂，论文报告真实差距，不删行。

---

## 3. 按天

### 第 0 周：规则、数据、基线、引言

| 日 | 日期 | 上午（实验/数据） | 下午（实验/数据） | 晚上（论文） | 当日验收 |
|----|------|-------------------|-------------------|--------------|----------|
| 1 | 09-22 二 | 写 3 档规则：每档 3 条可见特征，加边界例（划痕尺度、是否露底、污渍算不算伤） | 跑通环境，填 `experiments/00_env_check.md`：torch、CUDA、一条跑完的命令、一个 val 准确率 | 建 `paper/` 模板壳（题名到结论的空节）；用中文写下 3 条贡献主张 | 规则成文；环境有数字；模板能出 PDF |
| 2 | 09-23 三 | 下载 **1** 个带框或掩膜的公开集（NEU-DET 或 MVTec 一类）。记录 URL、许可、原始类别张数 | 写映射表：每个原标签 → 无/轻/中重，每行一句有序理由。没有框/掩膜才改用类激活裁剪，并记入局限 | 笔记 2 篇（缺陷检测 1、有序分类 1），格式按 `notes/papers/README.md` | 数据在 `data/`；映射表完成；笔记 2 篇 |
| 3 | 09-24 四 | 划分脚本：种子 42；train/val/test 比例写死；各类张数列表 | 同一种子生成两次，划分文件一致 | 该集上训练 ≥5 epoch。笔记再 2 篇（成色或质检） | 划分记录 + 训练日志里的 val 准确率；笔记累计 4 |
| 4 | 09-25 五 | 多数类基线：test Accuracy、macro-F1 | ImageNet 预训练 ResNet-18 微调跑完。epoch、学习率、输入尺寸、batch 写进实验记录 | 引言提纲：背景、缺口、3 条贡献，中英各一段 | CSV 有多数类和微调两行。若微调不高于多数类，记下只改一个因素的假设 |
| 5 | 09-26 六 | 只改昨天记下的那一个因素，重跑微调 | 测试集混淆矩阵、每档 F1、每档样本数 | 方法 3.1 英文：任务定义，规则与映射表逐句对应，不另写一套档 | 至少一版微调 test Accuracy；混淆矩阵图 |
| 6 | 09-27 日 | 主微调换第二个种子再跑 | 有序误差：MAE、差一档比例、差两档比例（两个种子都算） | 相关工作 2.1 草稿：表面缺陷，正文引用 ≥6 篇 | 两种子的 Accuracy；有序误差三个数 |
| 7 | 09-28 一 | 若两种子均值仍不超过多数类：只加类别权重或采样，再出一版。实验臂不删 | M1 表 v1：多数类、种子 1、种子 2。文件 `results/20260928_m1_*.csv` | 引言英文写完（≥4 段）。笔记累计 ≥8 | **周验收第 0 周** |

### 第 1 周：对照、定位再分级、方法章

| 日 | 日期 | 上午 | 下午 | 晚上 | 当日验收 |
|----|------|------|------|------|----------|
| 8 | 09-29 二 | 冻结骨干，线性探测跑完 | 同一测试集：Accuracy、macro-F1、每档 F1、有序误差 | 实现细节表：骨干、输入、lr、batch、epoch、增强、种子、GPU | 主表新增线性探测 |
| 9 | 09-30 三 | 同一骨干，有序损失对交叉熵开跑 | 跑完并评测同一组指标 | 方法 3.2 英文：整图分类与有序损失，公式和实现一致 | 交叉熵、有序损失各一行 |
| 10 | 10-01 四 | 第三对照：强增广或 MobileNetV3/EfficientNet-B0，预算与微调相同 | 合成主表：多数类 + 线性探测 + 微调 + 这一行 | 相关工作 2.2：有序分类，引用 ≥5 篇 | 主表 ≥4 行，列齐全 |
| 11 | 10-02 五 | 定位模型开跑（有框用检测，有掩膜用分割） | 验证集可视化 10 张：框或掩膜是否盖住伤。有框/掩膜则计算 mAP@0.5 或 IoU | 方法 3.3 提纲：裁剪或遮罩后如何进入分类器 | `results/` 有 10 张定位可视化 |
| 12 | 10-03 六 | 定位结果裁剪或遮罩后，送入已选定的整图模型，测试集评测 | 与最佳整图模型对比 Accuracy、macro-F1、每档 F1、有序误差 | 尺度分组规则写死（例如缺陷面积占图比），出小/大两组指标 | 定位臂一行 + 分组表 |
| 13 | 10-04 日 | 补消融：有序损失开/关、增广开/关 | 主表和消融的每档 F1 补全 | 相关工作 2.3：成色/外观质检，引用 ≥5 篇 | 消融 ≥2 个开关；相关工作三节都有稿 |
| 14 | 10-05 一 | 核对测试集 ID 一致；换过机器的结果分表，不合并 | 方法三节串成一章；图 1 流水线（整图支路 + 定位支路） | 三条贡献改成与现有数字一致的句子，负结果保留 | **周验收第 1 周** |

### 第 2 周：失败案例、真实小集、实验章、冻结

| 日 | 日期 | 上午 | 下午 | 晚上 | 当日验收 |
|----|------|------|------|------|----------|
| 15 | 10-06 二 | 导出测试集全部分错样本：路径、真值、预测、档差 | 逐张标类型：反光或脏纹理误报、小裂纹漏检、背景当伤、相邻档、跨两档、其他 | 每类至少 2 张，候选 ≥12，每张一句原因 | 失败清单 CSV |
| 16 | 10-07 三 | 写真实拍摄协议：同一类产品；正常光/反光；干净背景/杂乱背景；档规则沿用第 1 天 | 采集 ≥90 张，每档 ≥30。文件名含档位和拍摄条件 | 第二人按同一规则标注其中 ≥30 张（每档 10），两人互盲 | 90 张在 `data/real/`；协议一页；双标 30 张 |
| 17 | 10-08 四 | 算原始一致率与 Cohen's kappa | 用冻结候选中的最佳整图模型评这 90 张。不混入公开测试集，不据此换模型 | 相对公开测试集的 Accuracy、macro-F1 差值；反光组与杂乱背景组分开 | kappa；真实集指标表 |
| 18 | 10-09 五 | 从 12 张候选里定论文图版 ≥8，四类都有；图注含真值、预测、类型 | 混淆矩阵终图两张：最佳整图、定位再分级 | 实验章前半：数据、映射、划分、指标、实现细节、主表 | 图进 `paper/figures/`；实验前半无占位 |
| 19 | 10-10 六 | 实验章写入消融和有序误差 | 写入定位臂和尺度分组 | 写入失败类型计数及占错误的比例 | 上述三块每张表旁注明 CSV 文件名 |
| 20 | 10-11 日 | 实验章写入真实集、一致率、分组掉点 | 局限：反光、跨产品、主观性、映射是否等于成色。每点能指向一个表或图 | 结论；摘要 v1（150–250 词，含最佳数字和一处负结果或掉点） | 摘要数字全部可回溯 |
| 21 | 10-12 一 | 主表、消融、定位臂、真实集、有序误差冻结。表头写冻结日 **2026-10-12** | 全文数字与 CSV 逐格对照 | bib ≥25 条，2021 及以后 ≥8 条；正文引用能编译 | **周验收第 2 周。此后不换测试集、不改数字** |

### 第 3 周：全文、图表、复现

| 日 | 日期 | 上午 | 下午 | 晚上 | 当日验收 |
|----|------|------|------|------|----------|
| 22 | 10-13 二 | 按节合并进模板 | 去掉 TODO、中文残留句、占位图 | 题名改到可投稿粒度（对象 + 3 档损伤分级）；关键词 4–6 个 | 全文 PDF v1，记录页数 |
| 23 | 10-14 三 | 图 1 重出为矢量或 ≥300 dpi | 表的小数位全文统一；表题含指标和划分 | 每张图、每张表在正文至少引用一次 | 图 ≥4、表 ≥3，清单打勾 |
| 24 | 10-15 四 | 相关工作每段补上「现有工作缺什么、本文哪张表对应」 | 引用补到清单标准；删掉不能在出版方官网核到的文献 | 贡献段不再出现「提出新网络」 | 相关工作没有零引用段落 |
| 25 | 10-16 五 | 写 `experiments/INDEX.md`：主表每一行的命令、种子、输出 CSV | `requirements.txt` 锁版本；种子出现在命令里 | 按索引核对主表一行能从命令走到 CSV | 索引覆盖主表每一行 |
| 26 | 10-17 六 | 改引言和方法的英文：一句一个判断，主张与表一致 | 改实验和结论 | 摘要和结论不出现正文没有的数字 | 一页修改记录：改过的主张 |
| 27 | 10-18 日 | 查 2 个候选出处：截稿日、页数、模板、核对该届 EI 的官方页面。截稿早于 2026-10-22 的去掉 | 锁定 1 个，套它的模板 | 超页只删句子和空白。完整清单里的实验种类都还在正文 | 模板 PDF，页数合规 |
| 28 | 10-19 一 | 走第 4 节自查清单 | 能送老师就送出 PDF v2；当天能改的笔误和图注先改 | 保存 PDF v2 | **周验收第 3 周** |

### 第 4 周：终稿与投稿

| 日 | 日期 | 上午 | 下午 | 晚上 | 当日验收 |
|----|------|------|------|------|----------|
| 29 | 10-20 二 | 处理自查和已返回意见中的笔误、图注、表述 | 参考文献格式改成目标模板 | 检查图在 PDF 里可辨认（混淆矩阵数字、失败图伤口） | PDF v3；意见表（改/不改及原因） |
| 30 | 10-21 三 | 只读 PDF 一遍，对照冻结 CSV | 投稿表单：题名、摘要、作者、分类、关键词与 PDF 一致 | 源文件包：tex、图、bib。不含 `data/` 大文件，不含密钥 | 上传清单 |
| 31 | 10-22 四 | 提交 | 保存回执编号、时间；核对系统中的 PDF 页数 | `STATUS.md` 改为已投，写明冻结文件列表 | **回执。当天不开新实验** |

---

## 4. 交稿自查

- 测试集未参与选模；真实小集未参与选模  
- 有多数类基线；主表 ≥4 行；定位臂有对比；消融有两个开关  
- 每档 F1、混淆矩阵、有序误差都在  
- 失败图版 ≥8；真实集有 kappa 和掉点  
- 负结果还在正文里  
- 摘要、表格、结论三个地方的数字一致  
- 命令能对上主表  
- 参考文献 ≥25，近五年 ≥8  
- 局限四项都写了  
- 页数在模板限制内，且第 1 节清单没有被删掉  

---

## 5. 时间预算

| 块 | 小时 | 落在哪些天 |
|----|------|------------|
| 环境、划分、基线 | 40 | 1–7 |
| 对照、有序损失、消融 | 32 | 8–10、13 |
| 定位再分级 | 24 | 11–12、14 |
| 失败案例与真实小集 | 32 | 15–17 |
| 读论文与相关工作 | 36 | 每天晚上的阅读，集中在 2–3、6、10、13、24 |
| 英文全文与改稿 | 64 | 7、14、18–23、26、28–30 |
| 排版、复现、投稿 | 20 | 25、27、29–31 |

合计约 248 小时。
